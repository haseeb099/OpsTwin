# Cline

| File | Purpose |
|---|---|
| `.clinerules` | Cline project rules |
| `.opstwin/rules.md` | Full audit schema |

## Usage

1. Init OpsTwin in your repo
2. Cline loads `.clinerules` automatically
3. Set `"agent": "cline"` in the audit JSON
